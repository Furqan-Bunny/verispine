{
  "name": "verispine-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "VeriSpine Joint Centers — medical products & machinery marketplace and auction platform",
  "scripts": {
    "start": "node backend/start.js",
    "dev": "cd frontend && npm run dev",
    "dev:api": "cd backend && npm run dev",
    "build": "npm run clean && npm run install:all && npm run build:frontend",
    "clean": "cd frontend && rm -rf dist && cd ..",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "railway-build": "cd frontend && npm install --production=false && npm run build && cd ../backend && npm install",
    "railway-start": "node backend/start.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.11.0",
    "express-validator": "^7.0.1",
    "firebase-admin": "^11.11.0",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.5.3",
    "uuid": "^11.0.0"
  }
}
